/** Host loader entry for the browser-only outline plugin.
 * Zero-build package: this file is hand-maintained source AND the shipped artifact. */

/** Provides no host-side behavior. */
export function apply() {}
