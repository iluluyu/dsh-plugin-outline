# dsh-plugin-outline — one loader row over the shipped Web composition:
# the node half is a no-op; the browser half (dsh.client row) registers the
# outline navigation entry into the shell.overlay seat.
- insert:
    - id: outline
      name: 'dsh-plugin-outline'
